# simple-rag-bot
A simple Retrieval-Augmented Generation (RAG) application built using an LLM and a vector database. It retrieves relevant context from a knowledge base and combines it with the model’s reasoning to generate accurate, context-aware answers.
